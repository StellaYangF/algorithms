// 输入：nums = [2,7,11,15], target = 9
// 输出：[0,1]
// 解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。

export const addTwoNums = (nums: Array<number>, target: number): number[] => {
  const result = []
  for (let i = 0; i < nums.length; i++) {

  }

  return result
}

addTwoNums([2, 7, 11, 15], 9)